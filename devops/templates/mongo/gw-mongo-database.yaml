apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: gw-mongo-database
spec:
  selector:
    istio: {{namespace}}-egress
  servers:
    - hosts:
        - {{mongo_db_host}}
      port:
        name: tcp-11112
        number: 11112
        protocol: TCP